# 坦克大战 · 科技纪元 —— 网页小游戏技术文档

> 本文档面向 **solo builder（代码生成 agent）** 编写，目标是：拿到本文档即可直接生成一个**可运行、玩法完整、视觉科技感强、含 Boss 战**的网页坦克大战游戏。
>
> 技术栈：**HTML + CSS + Vanilla JavaScript（ES6+）+ Canvas 2D**，无第三方依赖，可直接用 VS Code + Live Server 打开运行。

---

## 0. 需求确认（本次必须全部满足）

| # | 需求 | 落点章节 |
|---|------|---------|
| 1 | 基本可玩性：玩家可操控坦克**移动、发射炮弹** | §7、§11 |
| 2 | 存在**敌方坦克 / 障碍物**，具备**碰撞检测**与**胜负判定** | §6、§9 |
| 3 | 游戏界面清晰，**操作反馈明确**（命中、震动、音效、HUD） | §10、§12 |
| 4 | 底图直接引用 `坦克大战的背景.jpg`（已存在本目录，尺寸 **1260 × 740 px**） | §5.1、§6 |
| 5 | 游戏**中央区域**地图布局（砖墙/障碍物）拼成作者姓名缩写 **LSQ** | §6.5 |
| 6 | 若技术受限无法用地图布局实现，则在**底图/界面显著位置**加 **LSQ** 标识 | §6.5 Fallback |

---

## 1. 项目概述

### 1.1 一句话描述
一款采用**赛博朋克 / 科技霓虹风格**的单机网页坦克大战，玩家操控坦克抵御波次进攻、保护己方基地，并在关底迎战拥有多阶段技能的大型 Boss。

### 1.2 玩法亮点（对市场爆款玩法的吸收）
| 玩法来源 | 我们的实现 |
|---------|-----------|
| 经典坦克大战 | 俯视角网格地图、波次刷怪、保护基地（老巢）、砖墙/钢墙地形 |
| 魂类 Boss 战 | 关底 Boss 拥有多阶段、多种弹幕/召唤技能、可见血条 |
| Roguelike 成长 | 击杀掉落随机道具，玩家可叠加火力/速度/护盾，形成 build |
| 弹幕/爽感 | 连击计数、爆炸粒子、屏幕震动、穿透弹、清屏炸弹 |

### 1.3 核心循环
```
开始 → 进入关卡(波次 1) → 击退敌方波次 → 掉落道具 → 波次推进
     → 每 5 波出现 Boss → 击败 Boss → 进入下一章节(难度提升)
     → 基地被毁 / 玩家生命归零 → 游戏结束
```

---

## 2. 技术选型与环境

| 项目 | 选择 | 说明 |
|------|------|------|
| 渲染 | HTML5 Canvas 2D | 游戏主画面 |
| 语言 | ES6+ (模块化) | 使用 `<script type="module">` 或直接多文件 |
| UI | HTML + CSS | HUD、菜单、结算页用 DOM/CSS，画面用 Canvas |
| 音效 | Web Audio API（程序化合成） | 不依赖音频文件，纯代码生成音效 |
| 依赖 | 无 | 所有资源用代码绘制/合成 |
| 运行 | VS Code + Live Server | 静态服务器即可打开 `index.html` |

---

## 3. 文件结构

```
坦克大战/
├── index.html              # 入口，挂载 Canvas 与 HUD
├── css/
│   └── style.css           # 页面布局、HUD、菜单、科技风样式
├── js/
│   ├── main.js             # 入口：初始化、游戏循环、状态机
│   ├── config.js           # 所有常量/配置（尺寸、颜色、数值平衡）
│   ├── input.js            # 键盘输入管理
│   ├── map.js              # 地图生成、地形数据、LSQ 布局、波次关卡定义
│   ├── entities.js         # 坦克/子弹/道具/Boss 等实体类
│   ├── ai.js               # 敌方 AI 与 Boss 行为
│   ├── collision.js        # 碰撞检测
│   ├── particles.js        # 粒子/爆炸/光效系统
│   ├── audio.js            # Web Audio 程序化音效
│   └── ui.js               # HUD 更新、菜单、结算页
├── 坦克大战的背景.jpg       # 游戏底图（已存在，直接引用，1260×740）
└── 坦克大战-技术文档.md
```

> **Builder 提示**：若希望单文件交付，可将上述模块合并为一个 `game.js`，但建议保留模块边界以利于维护。

---

## 4. 整体架构

### 4.1 状态机
游戏主体由 `Game` 对象驱动，包含以下状态：

```
MENU → PLAYING → (PAUSED) → BOSS_INTRO → GAME_OVER → VICTORY
```

- `MENU`：开始界面（标题 + 开始按钮 + 说明）
- `PLAYING`：主游戏循环中
- `BOSS_INTRO`：Boss 出场播报（血条 + 名称 + 警告动画）
- `GAME_OVER` / `VICTORY`：结算页（得分、击杀、连击纪录）

### 4.2 游戏主循环（`requestAnimationFrame`）
```js
function gameLoop(timestamp) {
  const dt = (timestamp - lastTime) / 1000; // 秒，用于帧率无关更新
  lastTime = timestamp;

  update(dt);   // 1. 更新坦克/子弹/AI/道具/粒子
  render();     // 2. 绘制到 Canvas
  requestAnimationFrame(gameLoop);
}
```

> 所有移动、冷却、动画都基于 `dt` 计算，保证不同刷新率下速度一致。

---

## 5. 核心配置（config.js）

### 5.1 画布与网格
- 游戏画面尺寸：**1260 × 740 px**（与底图 `坦克大战的背景.jpg` 完全一致，避免拉伸变形）
- 网格单元（tile）：**20 × 20 px** → 游戏区域 **63 × 37 格**
- 画布通过 CSS `max-width: 100%` 等比缩放适配不同屏幕，内部坐标恒用 1260×740 逻辑坐标
- 顶部 HUD 单独用 DOM 渲染，不占 Canvas 空间
- 底图加载方式：`main.js` 启动时 `new Image(); img.src = '坦克大战的背景.jpg';` 加载完成后作为 Canvas 最底层绘制（详见 §6.1）

### 5.2 数据结构（枚举）
```js
const DIR = { UP: 0, RIGHT: 1, DOWN: 2, LEFT: 3 };

const TILE = {
  EMPTY: 0,   // 空地
  BRICK: 1,   // 砖墙（可被子弹破坏）
  STEEL: 2,   // 钢墙（仅穿透弹/高火力可破坏）
  WATER: 3,   // 水域（坦克不可过，子弹可过）
  GRASS: 4,   // 草地（坦克可过，会遮挡战车）
  BASE: 5,    // 基地/老巢（被摧毁则失败）
};
```

### 5.3 数值平衡建议（可调）
| 参数 | 默认值 | 说明 |
|------|--------|------|
| 玩家基础速度 | 120 px/s | |
| 玩家基础血量 | 3 | 被击中扣 1 |
| 玩家子弹冷却 | 0.35s | |
| 子弹速度 | 320 px/s | |
| 敌方基础血量 | 1 | |
| Boss 血量 | 150 | 分 3 阶段 |
| 每章波次 | 5 波 | 第 5 波出 Boss |

---

## 6. 地图与关卡设计

### 6.1 地图表示
用二维数组 `grid[row][col]` 存储地形，`row` 为纵坐标（0~36），`col` 为横坐标（0~62）。

底图 `坦克大战的背景.jpg` 作为 Canvas 最底层绘制，游戏的地形块（砖墙/钢墙/水域/草丛/基地）绘制在其上层：

```js
// 每帧渲染顺序
ctx.drawImage(bgImg, 0, 0, 1260, 740);   // 1. 底图
drawGrid();                               // 2. 地形块（墙/水/草/基地）
drawEntities();                           // 3. 坦克/子弹/道具（草丛之上的实体后处理遮挡）
drawParticles();                          // 4. 粒子特效（最上层）
```

### 6.2 地图要素
- **不可通行**：`BRICK`、`STEEL`、`WATER`、`BASE`
- **可通行**：`EMPTY`、`GRASS`
- **遮挡渲染**：`GRASS` 绘制在坦克上层（半透明遮罩）形成"躲进草丛"效果

### 6.3 地图生成建议
- 地图**正中央**用砖墙拼出作者姓名缩写 **LSQ**（见 §6.5，这是地图标志性布局）
- 四角设为敌方出生点，玩家出生点在地图正下方
- 基地固定在地图**正下方中央**，四周用砖墙围成"鹰巢"，留一个入口
- 钢墙点缀在 LSQ 周围及关键路口（增加策略性）
- 水域分布在两侧，草地散布在地图空位
- 确保 LSQ 布局、基地、出生点之间留出足够通行空间，避免开局即卡死

### 6.4 波次定义
```js
const CHAPTERS = [
  { waves: 5, spawnRate: 1.0, enemyTypes: ['scout','regular'] },
  { waves: 5, spawnRate: 1.2, enemyTypes: ['regular','rapid','heavy'] },
  { waves: 5, spawnRate: 1.5, enemyTypes: ['rapid','heavy','elite'] },
];
```
每波敌人数量递增；波次间有 3 秒间隔；每章结束进入更难的一章。

### 6.5 姓名缩写 LSQ 地图布局（中心区域，需求 5/6）
在游戏地图**正中央**用砖墙（`TILE.BRICK`）拼出作者姓名缩写 **LSQ**，作为地图标志性障碍布局，兼顾美观与作者署名。

- 每个字母使用 **5 列 × 7 行** 像素矩阵定义：`1` = 砖墙，`0` = 空地
- L / S / Q 三字母**水平排列**，字母间留 2 格空隙
- 居中定位：先计算三字母总宽度，再水平居中到网格中心（`centerCol = 31`，`centerRow = 18`）

像素矩阵（1 = BRICK）：

```
L:          S:              Q:
10000       01111           01110
10000       10000           10001
10000       10000           10001
10000       01110           10001
10000       00001           10011
10000       00001           01101
11111       11110           00001
```

生成函数（写入 `map.js`）：
```js
const LETTERS = {
  L: ['10000','10000','10000','10000','10000','10000','11111'],
  S: ['01111','10000','10000','01110','00001','00001','11110'],
  Q: ['01110','10001','10001','10001','10011','01101','00001'],
};

function stampLSQ(grid, centerRow = 18, centerCol = 31) {
  const word = ['L', 'S', 'Q'];
  const spacing = 2;                 // 字母间空隙（格）
  const letterW = 5, letterH = 7;
  const totalW = word.length * letterW + (word.length - 1) * spacing;
  let col = centerCol - Math.floor(totalW / 2);
  for (const ch of word) {
    const px = LETTERS[ch];
    for (let r = 0; r < letterH; r++) {
      for (let c = 0; c < letterW; c++) {
        if (px[r][c] === '1') {
          grid[centerRow - 3 + r][col + c] = TILE.BRICK;
        }
      }
    }
    col += letterW + spacing;
  }
  return grid;
}
```

> **Fallback（需求 6 备用方案）**：若因技术限制无法在地图布局中实现 LSQ 字母墙，则**必须**在游戏底图或界面显著位置（如 HUD 标题、开始菜单标题、画面角落水印）加入姓名缩写 **LSQ**，以证明作品为作者本人独立完成。优先满足地图布局方案，Fallback 仅作兜底。

---

## 7. 实体设计（entities.js）

### 7.1 玩家坦克（Player）
```js
class Player {
  x, y, dir, speed, hp, maxHp,
  fireLevel,      // 1-3：子弹数量/威力
  fireCooldown,
  shieldTimer,    // 护盾剩余时间
  // 方法：update(dt, input), fire(bullets)
}
```
- 移动方向由 `input.js` 提供，发射键为 **空格 / J**
- 移动时限制在可通行区域（与地图碰撞检测）
- 出生 3 秒内带护盾（防止被围殴）

### 7.2 子弹（Bullet）
```js
class Bullet {
  x, y, dir, speed, owner,      // owner: 'player' | 'enemy'
  pierce,       // 是否可穿透
  damage,
}
```
- 子弹撞墙逻辑：`BRICK` 子弹消失+墙破坏；`STEEL` 子弹消失（穿透弹可穿过）；`WATER` 子弹穿过；`BASE` 基地被毁
- 玩家子弹与敌方子弹**可互相抵消**（经典爽点）

### 7.3 敌方坦克（Enemy）
```js
class Enemy {
  type, x, y, dir, speed, hp, score,
  ai,            // AI 行为引用
  dropRate,      // 死亡掉落道具概率
}
```
敌人类型：

| 类型 | 速度 | 血量 | 特点 |
|------|------|------|------|
| scout 侦察兵 | 快 | 1 | 快速移动、低血量 |
| regular 普通兵 | 中 | 1 | 基础单位 |
| rapid 快速射手 | 中 | 1 | 射速快 |
| heavy 重装兵 | 慢 | 3 | 血量高、碰撞体积大 |
| elite 精英兵 | 中 | 2 | 可发射三连弹 |

### 7.4 道具（PowerUp）
```js
const POWERUP = {
  POWER:   '火力增强（子弹变强/变多）',
  SPEED:   '加速',
  SHIELD:  '护盾（无敌 5 秒）',
  LIFE:    '恢复 1 点生命',
  BOMB:    '全屏清怪',
  PIERCE:  '穿透弹（限时）',
};
```
- 敌人死亡后按 `dropRate`（默认 20%）随机掉落一个道具
- 道具用发光图标 + 旋转动画绘制，被玩家吃到即生效

---

## 8. Boss 设计（核心亮点）

### 8.1 出场
进入 Boss 波时，状态切到 `BOSS_INTRO`：显示 Boss 名称（如「毁灭者 D-9」）+ 顶部大型血条 + 红色警告闪烁 + 低音音效。

### 8.2 Boss 属性
```js
class Boss {
  x, y, hp, maxHp, phase,      // phase: 1|2|3
  moveTimer, attackTimer,
}
```
- 体积约为普通坦克 **2×2 格（40×40 px，可放大至 3×3 = 60×60 px 以增强压迫感）**，移动缓慢
- 血量分 3 阶段：`>66%` 阶段 1，`33%~66%` 阶段 2，`<33%` 阶段 3
- 血条实时显示在画面顶部（DOM）

### 8.3 Boss 三阶段攻击模式
| 阶段 | 攻击方式 |
|------|---------|
| 阶段 1（满血） | 直线瞄准弹 + 缓慢移动 |
| 阶段 2（中血） | 扇形散射弹（5 发）+ 召唤两只精英小兵 |
| 阶段 3（残血） | 360° 环形弹幕 + 副炮扫射 + 狂暴加速 |

- 每进入新阶段触发一次**全屏闪烁 + 震动 + 音效**
- Boss 死亡：爆炸粒子特效 + `VICTORY` 或进入下一章

### 8.4 Boss AI 伪代码
```js
function bossUpdate(dt) {
  // 缓慢朝玩家移动，保持距离
  if (phase === 1) {
    attackTimer -= dt;
    if (attackTimer <= 0) { fireAimedShot(player); attackTimer = 1.5; }
  } else if (phase === 2) {
    fireSpreadShot(5, 40deg);           // 扇形
    if (spawnTimer <= 0) { spawnElite(); spawnTimer = 8; }
  } else {
    fireRing(24);                        // 环形弹幕
    moveFaster();
  }
}
```

---

## 9. 碰撞检测（collision.js）

### 9.1 关键规则
1. **坦克 vs 地图**：根据移动方向计算目标格是否可通行，不可通则停止该方向移动
2. **坦克 vs 坦克**：矩形重叠检测（AABB），阻挡或反弹
3. **子弹 vs 地图**：按 tile 类型处理（见 7.2）
4. **子弹 vs 坦克**：AABB 检测，命中扣血、生成火花粒子
5. **子弹 vs 子弹**：同格碰撞则双方消失

### 9.2 AABB 碰撞辅助函数
```js
function aabb(a, b) {
  return a.x < b.x + b.w && a.x + a.w > b.x &&
         a.y < b.y + b.h && a.y + a.h > b.y;
}
```

---

## 10. 科技风视觉规范（重点：页面美观）

### 10.1 配色方案（赛博朋克霓虹）
| 用途 | 颜色 |
|------|------|
| 背景深色 | `#0a0e17` / `#0d1421` |
| 网格线 | 半透明 `rgba(0, 240, 255, 0.08)` |
| 主色调（玩家/UI） | 青色 `#00f0ff` |
| 强调色（敌人/Boss） | 品红 `#ff2d78` / 橙红 `#ff6b35` |
| 成功/道具 | 绿色 `#39ff88` / 金色 `#ffd166` |
| 警告/危险 | 红色 `#ff2d55` |

### 10.2 视觉特效（用 Canvas 实现）
1. **发光（Glow）**：关键物体绘制时设置 `ctx.shadowBlur` 与 `ctx.shadowColor`（如坦克轮廓、子弹、道具）
2. **霓虹描边**：主体用深色填充 + 高亮描边
3. **动态网格背景**：在底图之上叠加一层半透明、缓慢流动/呼吸的霓虹网格线（增强科技感，不遮挡底图）
4. **粒子爆炸**：子弹命中/坦克爆炸生成 15~30 个霓虹粒子，带拖尾与衰减
5. **屏幕震动**：爆炸/Boss 阶段切换时对 Canvas 施加小幅随机偏移
6. **淡入转场**：菜单→游戏、Boss 出场时使用遮罩过渡

### 10.3 HUD 与菜单（DOM + CSS）
- 顶部/侧边 HUD：生命（心形或能量条）、得分、波次、火力等级、护盾状态
- 使用无衬线字体 + `letter-spacing` + 发光 `text-shadow` 营造科技感
- 开始菜单：居中标题「坦克大战 · 科技纪元」，霓虹描边，按钮悬停发光
- 结算页：展示得分 / 击杀数 / 最高连击

---

## 11. 输入处理（input.js）

- 支持 **方向键（WASD / 方向键）** 控制移动
- **空格 / J** 射击
- **P / Esc** 暂停
- 支持**按住连续移动/射击**（用 `keydown` 记录按键状态，`keyup` 释放）
- 记录按键状态映射对象 `keys = { up:false, left:false, ... }`，`update` 里读取

---

## 12. 音效（audio.js，Web Audio API）

不依赖音频文件，用振荡器合成：
- 射击：短促方波 `340Hz → 快速衰减`
- 爆炸：白噪声 `+低频正弦` 衰减
- 拾取道具：上行音阶三角波
- 护盾：高频正弦脉冲
- Boss 出场：低频锯齿波 + 渐强
- 游戏结束：下行音阶

> 需在用户首次点击（开始按钮）后初始化 `AudioContext`，规避浏览器自动播放限制。

---

## 13. 实现顺序建议（Builder 执行路径）

1. `config.js` → 常量与枚举（含底图路径、1260×740、tile=20）
2. `map.js` → 地图数据与绘制（含 `stampLSQ` 生成中心 LSQ 砖墙布局）
3. `input.js` → 输入管理
4. `entities.js` → Player / Bullet / Enemy / PowerUp
5. `collision.js` → 碰撞（含胜负判定：基地/玩家）
6. `ai.js` → 敌方 AI 与 Boss
7. `particles.js` → 粒子与特效
8. `ui.js` + `css/style.css` → HUD、菜单、结算
9. `audio.js` → 音效
10. `main.js` → 加载底图 `坦克大战的背景.jpg` + 状态机 + 主循环整合

---

## 14. 验收标准（Definition of Done）

**基础可玩性（需求 1-3）**
- [ ] 用 Live Server 打开能直接运行，无报错
- [ ] 玩家可**移动、发射炮弹、转向**流畅
- [ ] 存在**敌方坦克与障碍物**，具备**碰撞检测**
- [ ] 具备**胜负判定**：基地被毁 / 玩家生命归零 = 失败；击败 Boss = 胜利
- [ ] 界面清晰，**操作反馈明确**（命中火花、爆炸震动、拾取提示、音效）

**玩法完整**
- [ ] 敌方波次逐步推进，AI 能移动并攻击
- [ ] 砖墙可被击毁、钢墙不可、水域不可通行、草丛可藏身
- [ ] 基地被摧毁后游戏结束；基地有保护判定
- [ ] 道具可掉落并可拾取生效（火力/护盾/生命/清屏等）
- [ ] 每 5 波出现 Boss，Boss 有三阶段攻击模式与血条
- [ ] 击败 Boss 后进入下一章或胜利结算

**视觉与署名（需求 4-6）**
- [ ] 底图已引用 `坦克大战的背景.jpg`（1260×740），画面无拉伸变形
- [ ] 地图**正中央**砖墙拼出 **LSQ** 姓名缩写，清晰可辨
- [ ] 若地图布局未实现 LSQ，则在底图/界面显著位置有 **LSQ** 标识
- [ ] 科技风：霓虹发光、粒子爆炸、动态网格背景、屏幕震动
- [ ] 有完整的菜单 / HUD / 暂停 / 结算流程
- [ ] 有程序化音效；不同刷新率下运行速度一致

---

## 15. 可扩展方向（可选，非本次必须）

> 以下为后续迭代方向，本次交付不要求实现，仅预留扩展点（架构上保持模块解耦即可）。

| 方向 | 说明 | 涉及模块 |
|------|------|---------|
| 双人合作 | 第二玩家同屏操作（WASD / 方向键分别控制两名玩家，或支持本地联机） | `input.js`、`entities.js`、`main.js` |
| 本地最高分存档 | 用 `localStorage` 保存最高分、最高连击、通关章节数 | `ui.js`、`config.js` |
| 更多地形 | 传送门（瞬移）、可破坏钢墙、地雷、加速带、减速带 | `map.js`、`collision.js` |
| 更多 Boss 与章节主题 | 冰霜（减速弹）、火焰（灼烧/持续伤害）、电磁（EMP 禁用射击）等主题 Boss | `ai.js`、`entities.js`、`particles.js` |
| 成就/任务系统 | 击杀数、限时通关、无伤通关等成就 | `ui.js`、`main.js` |
| 武器系统深化 | 机枪、激光、导弹、散射炮、追踪弹等武器升级 | `entities.js`、`particles.js` |
| 关卡编辑器 | 自定义地图布局并导出/导入 | `map.js`、`ui.js` |

设计原则：**核心循环保持不变**，扩展时优先新增「类型/配置」，而非修改既有逻辑（体现开闭原则）。

---

*文档结束。可直接交予 builder，按「第 13 节」顺序生成代码。*